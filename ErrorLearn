firebase config error- i imported older way to import auth firestore(compat), i should i mport react-native way

// if firebase creation error - firebaseConfig file is not created , not properly imported
// if firebase imported error- remove node modules, reinstall from  npm install command